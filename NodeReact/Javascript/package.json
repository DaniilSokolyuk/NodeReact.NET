{
  "version": "1.0.0",
  "name": "NodeReact",
  "private": true,
  "scripts": {
    "build": "echo ------ Yarn Install ------ && yarn install && echo ------ Webpack Compile ------ && webpack",
    "watch": "webpack --watch"
  },
  "dependencies": {
    "filewatcher": "3.0.1",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  }
}
